@page "/Lists/{UserDisplayName}/{ListName}/{ListID:int}"

@if(model is null)
{
	
}
else
{
	<MudText>@model.Name</MudText>
	@foreach(var drink in model.Drinks)
	{
		<MudField>@drink.Name</MudField>
	}
}

