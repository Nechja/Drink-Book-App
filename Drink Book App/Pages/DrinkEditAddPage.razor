@page "/adddrink"
@page "/editdrink"
@attribute [Authorize(Roles = "Admin")]

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
            <div class="navbar-brand"></div>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <MudButton Variant="Variant.Text" OnClick="() => menulocation = 0">Drink</MudButton>
                </li>
                <li class="nav-item">
                      <MudButton Variant="Variant.Text" OnClick="() => menulocation = 1">Instruction</MudButton>
                </li>
                <li class="nav-item">
                      <MudButton Variant="Variant.Text" OnClick="() => menulocation = 2">Ingredient</MudButton>
                </li>
                <li class="nav-item">
                    <MudButton Variant="Variant.Text" OnClick="() => menulocation = 3">Ingredient Type</MudButton>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="#">Glassware</NavLink>
                </li>
            </ul>
        </div>
    </div>
</nav>


<MudPaper>
    @if (menulocation == 1)
    {
        
    }
    @if (menulocation == 2)
    {
        <AEIngredientCard></AEIngredientCard>
    }
    @if (menulocation == 3)
    {
        <AEIngredientTypeCard></AEIngredientTypeCard>
    }
    
</MudPaper>
        







@code {
    int menulocation = 0;
}
